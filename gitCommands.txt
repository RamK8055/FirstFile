git clone "url_of_the_project"		- clone the project
git fetch				- pull(download) all the branches newly created by others
git pull				- pull(download) all the codes newly created by others
cd "folder_name"			
git touch "filename.ext"		- create a new file
ls
git status				- check newly added or modified files
git add "filename.ext"			- add perticular file to commit
git add * 				- add all files to commit
git status
git commit -m "Desc_for_commit"		- commit the changes
git status
git log					- to view the log of all commit
git push -u origin master		- push(upload) our code to Git Repository

//changes made to test commit